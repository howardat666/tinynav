# X5 算法与硬件实测数据

> **本文只放数据和记录** —— 硬件占用、算法延迟/内存、BPU 分析、方案参数对比。
> **任务框架、里程计方案、路线图、待确认事项见 [`README.md`](README.md)。**
>
> 实测时间：2026-07-30 ~ 07-31 · 设备：3 台 15.7GB Looper + 1 台 64GB Looper
> 标记约定：**⬤ = 本人实测** · **○ = 估算或外推** · 未标记 = 引用他人数据或代码事实

---

## 索引

| 节 | 内容 | 想查什么就看这里 |
|---|---|---|
| [1](#1-结论摘要) | 结论摘要 | 一屏看完所有判断 |
| [2](#2-设备与硬件) | 设备与硬件 | 四台设备台账 · 64GB 故障诊断 · 内存/CPU/BPU/存储余量 · CPU 算力基准 |
| [3](#3-vio-负载) | VIO 负载 | VIO 占几核（静止 vs 运动）· 线程级拆分 |
| [4](#4-bpu-分析) | BPU 分析 | 占用率 · 帧率为何不可调 · **算子构成（谁能上 BPU）** |
| [5](#5-算法实测) | 算法实测 | **单模型延迟** · DINOv2 内存四档 · ORB · LightGlue 并行 · **BF 替代 LightGlue** |
| [6](#6-六个算法总表) | 六个算法总表 | **三层×六算法的资源/延迟总表** · recall 与用时的错位 |
| [7](#7-九个方案对比) | 九个方案对比 | **九配置总表** · PR #136 是哪个方案 · 逐方案细节 · 上板难度 |
| [8](#8-时延预算5-秒) | 时延预算 | 5 秒预算下谁塞得进去 |
| [9](#9-gpu-对照基准) | GPU 对照基准 | x86+TRT 的真实建图耗时 · 原版重定位频率到底多少 |
| [10](#10-检索质量与日夜数据) | 检索质量与日夜数据 | **day/night R@K** · 四个数据空白 |
| [11](#11-地图存储) | 地图存储 | 各方案地图多大 · 怎么瘦身 |
| [12](#12-依赖与部署障碍) | 依赖与部署障碍 | 板上缺什么 · pyproject 的坑 |
| [13](#13-上游分支盘点) | 上游分支盘点 | 四个分支/PR 各自做了什么、能复用什么 |
| [附录 A](#附录-a-我在相机上的改动) | 相机上的改动 | 我放了什么、怎么清理 |

**关键数据速查**：

| 问题 | 答案 |
|---|---|
| 可用内存 | ⬤ **~911–943 MB，无 swap**（64GB 版也一样）|
| 可用 CPU | ⬤ **4.5 核**（VIO 开）/ **6.6 核**（VIO 关）|
| SuperPoint @320×272 | ⬤ **1010 ms**（6 线程）· GPU 对照 **8.4 ms** |
| LightGlue（每对）| ⬤ **2870 ms** |
| DINOv2-base | ⬤ **5190 ms / 峰值 504 MB**（关 arena）|
| ORB 提取 + BF-Hamming | ⬤ **46.5 ms + 26.0 ms** |
| **cv2 BF-L2 匹配 SuperPoint 描述子** | ⬤ **38.6 ms**（比 LightGlue 快 **74 倍**）|
| 检索 R@1（day / night）| **88.87% / 31.21%**（DINOv2 patch VLAD）|
| BPU 占用 | ⬤ **87–95% 时间**，帧率**不可调** |

---

## 1. 结论摘要

| 判断 | 结论 |
|---|---|
| 里程计可插拔性 | 🟢 下游只认 `/slam/odometry` 等话题，换轮速**不用改** `planning_node`/`map_node` |
| 重定位周期要求 | 🟢 **5 秒足够**（Jetson 版实测约 3 s/次）—— 详见 [`README.md`](README.md) |
| SuperPoint + LightGlue 在 X5 CPU | 🔴 **15.1 s/次**（top_k=3），极限优化后仍 3.9–5.8 s，全程吃满 CPU |
| ORB + BF-Hamming 在 X5 CPU | 🟢 **0.39 s/次，47 MB，1–2 核** |
| DINOv2-base 在 X5 CPU | 🔴 5.19 s / 峰值 816 MB（关 arena 504 MB）/ **实际触发过 OOM**，会拖挂固件 |
| BPU | 🟡 时间占用 87–95%；🔴 **无 depth 帧率参数**（sensor mode 最低 20 fps），"降到 5 fps 释放 5.5 TOPS" **做不到** |
| VIO | 🟡 运动时占 **2.11 核**；关掉可让空闲从 4.5 升到 6.6 核。🔴 但固件里**不存在 VIO 开关**，需 Looper 放开 |
| BPU 与内存的关系 | 🔴 **BPU 与 CPU 共用同一块 DDR，没有独立显存** —— 上 BPU 不省内存 |
| 内存优化 | 🟢 关 ORT arena：816 → **504 MB，延迟不变**，零代价 |
| LightGlue 的处置 | 🟢 **不要加速它，而是不用它** —— cv2 BF 匹配 38.6 ms 替代 2870 ms |
| 存储 | 🟢 64GB 版 `/userdata` **53 G（剩 44 G）**，双时段地图够用；🔴 但内存一分没多 |
| 最优落点 | 🟢 **方案 6**：DINOv2 int8 上 BPU + SuperPoint CPU + cv2 BF ≈ **1.5 s / 480 MB** |

**核心矛盾与出路**：这颗芯片的 CPU 跑不动神经网络的局部特征匹配（SP+LG 要 15.1 s），而 BPU 被固件占着且帧率不可调。出路是**只把最该上 BPU 的模型上 BPU（DINOv2），把最贵的模型整个删掉（LightGlue）**。

---

## 2. 设备与硬件

### 2.1 四台设备台账

| | 桌面 A | 桌面 B | 北京狗挂载 | **64GB 改装版**（2026-07-31 实测）|
|---|---|---|---|---|
| `soc_uid` | 未记录 | ⬤ **`0x328152560a164b9402a4408a00120040`** | `...0a13d598039d4b790012004` | ⬤ **`0x328152560a164e920256c08a00120040`** |
| 固件 | 6.1.83-DR-PL5.2_V1.1.0 / 2.1.2 | 同 | 同 | **`LNX6.1.83_PL5.2_V1.1.0_20260717-1802`**（同内核同 PL）|
| eMMC 总量 | ⬤ 15.7 GB | ⬤ 15.7 GB | ⬤ 15.7 GB | 🟢 ⬤ **62.6 GB** |
| `/userdata` | ⬤ 9.2 G（剩 2.3–2.6 G）| 同 | 同 | 🟢 ⬤ **53 G（剩 44 G）** |
| `/` 与 `/app` | 2.0 G / 673 M | 同 | 同 | ⬤ **完全一致** |
| 内存 total / available | ⬤ 1307 / 908 MB | ⬤ 1307 / 912 MB | ⬤ 1307 / 866 MB | 🔴 ⬤ **1307 / 943 MB，swap 0** |
| CPU 核数 | 8 | 8 | 8 | ⬤ 8 |
| SuperPoint @320×272 | ⬤ 1050 ms | ⬤ 1010 ms | — | 未测（固件未启动）|
| LightGlue 512 kpts | ⬤ 2783 ms | ⬤ 2844 ms | — | 未测 |
| **相机固件状态** | 🟢 正常 | 🟢 ⬤ **三路全正常**：sif0 ipi0=30fps(RGB) · sif2=20fps(左目) · sif3=20fps(右目) · isp0/1=20 · isp2=30 | 🟢 正常 | 🔴 **failed，见 § 2.2** |
| bench 文件位置 | ❌ 不在这台 | ⬤ **`/userdata/tmp/` 在这台**（含 `pylibs/cv2` 4.6.0.66 + `stub/libgtk-3.so.0`）| — | ❌ 无 |

标准版三台推理延迟差异 **2–4%**，在噪声范围内 —— 结论不是偶然。

**64GB 版的三条结论**：

1. 🟢 **存储真的扩了** —— 分区表跟着改了，不是只换芯片。双时段（白天+夜晚）地图完全够用。
2. 🔴 **内存一分没多** —— 1307 MB total / 943 MB available / swap 0，**和标准版完全一样**。**所有方案的内存判断不变。**
3. 🔴 **系统盘没动** —— `/` 还是 2.0 G（剩 1.1 G），装系统级包仍然紧张。

⚠️ **`/etc/machine-id` 在所有设备上完全相同**（固件镜像烧入），认设备只能用 `soc_uid`：

```bash
cat /sys/class/socinfo/soc_uid     # 认设备
cat /sys/block/mmcblk0/size        # ×512 字节 = eMMC 总量
df -h /userdata                    # 标准版 9.2G / 64GB 版 53G
```

### 2.2 🔴 64GB 机器的 MIPI 故障 —— 已定位为**软件问题**（lane 数不匹配）

> ⚠️ **本节推翻了早前"物理连接问题"的判断。** 早前依据是"I2C 正常但 D-PHY 起不来 + 冷启动/重启都复现"，
> 看似指向排线。深挖后发现是 **sensor 被写成 1 lane、而 CSI host 被配成 2 lane**，host 永远等不到 D1 进入 LP-11。

**症状**：`insight_full` 启动后立即 abort，systemd 重试 5 次后放弃（`Start request repeated too quickly`）：

```
what():  CreateAndRunVflow failed, ret=-10
signal: SIGABRT (6)   thread: insight_full
  insight_full::CreateAndRunVflow(...)              ← 崩溃点
  insight_full::HobotFullNode::initCameras(...)
```

```
vin sensor0: sc132gs open i2c4@0x32                   ← ✅ I2C 上能识别到传感器
vin mipi2: 2 lane 1088x1280 20fps datatype 0x2b       ← ⚠️ host 配成 2 lane
vin mipi2: ipiclk set 600000000 get 600000000         ← ✅ 时钟锁定正常
vin mipi2: lane state of host phy is error: 0x1000d   ← 🔴 D1 通道起不来
vin mipi2: wait phy stop state error
vin mipi2: init error: -1
```

#### 相机拓扑（实测 + device tree）

| 端口 | vcon | i2c | 传感器 | 配置 lane | 状态 |
|---|---|---|---|---|---|
| mipi rx0 `0x3d060000` | vcon@0 | i2c-4 @0x1a | **imx415（RGB）** | 4 | ⚠️ **从未被初始化过，未测** |
| mipi rx1 `0x3d070000` | vcon@1 | — | 无（`snrclk: none`）| — | 未使用 |
| mipi rx2 `0x3d080000` | vcon@2 | i2c-4 @0x32 | **sc132gs 立体#1** | 2 | 🔴 2lane 失败 `0x1000d` / 🟢 **1lane OK** |
| mipi rx3 `0x3d090000` | vcon@3 | i2c-0 @0x32 (+EEPROM 0x50) | **sc132gs 立体#2** | 2 | 🔴 2lane 失败 `0x10001` / 🟢 **1lane OK** |

DPHY 分组：rx0/rx1 = group 0，rx2/rx3 = group 1，共用 `mipi_dphy@3d0a0000`。
配置来源：`/userdata/install/share/insight_full/config/user_params.json` → `stereo_sensor_name: sc132gs-1088x1280-20fps-2lane`。

#### 决定性实测：1 lane 下两路立体相机都完美工作

用板上自带的 `/app/platform_samples/sample_isp/multi_isp_vflow`（`-s N` 可同时拉 mipi2+mipi3）：

```
$ multi_isp_vflow -s 1      # 1 lane 配置
vin mipi3: 1 lane ... check phy stop state
vin sensor0: sensor_start sc132gs flow0 start done
vin mipi3: start cmd: 0 real        vin mipi2: start cmd: 0 real
sif2: ipi[0] fps:62                 sif3: ipi[0] fps:62
isp[0] fps:61                       isp[1] fps:62
mipi_host2 icnt 非零项：（无）      mipi_host3 icnt 非零项：（无）
```

**两路都跑满 ~62 fps，MIPI 错误计数器全 0。** 而 `-s` 用 2 lane 配置时 rx3 报 `0x10001`（同样是 D1 失败）。

#### `0x1000d` 的确切含义

寄存器可读路径：`/sys/class/vps/mipi_host{0..3}/status/regs`（`debugfs` 下只有 `sif0-3/fps`，无 mipi 节点）。

`0x04c` = DW MIPI CSI-2 host 的 **`PHY_STOPSTATE`**：`bit16` = clock lane，`bits[3:0]` = data lane 0..3。

```
shutdownz=0x1  dphy_rstz=0x1  N_LANES=0x1(=2lane)  PHY_RX=0x10000  PHY_STOPSTATE=0x1000d
```

🔑 **所有错误计数器全为 0**（`phy_fatal`/`phy`/`pkt_fatal`/`crc_frm_fatal`/`pld_crc_fatal`/`ecc_corrected`/`data_id`/`line`/`ipi`）→ **D1 不是"信号质量差"，而是从来没被驱动过。**

**和正常机器（桌面 B）的逐寄存器对照** —— 这才是决定性的：

| 寄存器 | 正常 host2（左目）| 正常 host3（右目）| 正常 host0（RGB）| **64GB 故障** |
|---|---|---|---|---|
| `0x004` **N_LANES** | **0x1（=2 lane）** | 0x1 | 0x3（=4 lane）| **0x1（=2 lane）→ 完全相同** |
| `0x04c` **PHY_STOPSTATE** | **0xf** | **0x3** | 0xf | 🔴 **0x1000d → 不同** |
| `0x008` CSI2_RESETN | 0x1 | 0x1 | 0x1 | — |
| `0x048` PHY_RX | 0x30000 | 0x30000 | 0x30000 | — |
| `icnt` 全部错误计数 | 全 0 | 全 0 | 全 0 | 全 0 |
| `mipi_phy` 参数 | lane=2, mipiclk 1200 Mbps, settle=20, laneclk=575 Mbps, init_cnt=1, **reset_cnt=0** | 同 | — | — |

> ⚠️ **修正早前对 `0x1000d` 的解码。** `bit16` = clock lane 的 **stop state**（LP-11 空闲）标志，**置 1 表示时钟 lane 停着、没在跑 HS**：
> - 正常机 `0xf` → **bit16 = 0 → HS 时钟在跑** ✅（数据 lane 位在包间隙置 1 是正常的）
> - 64GB `0x1000d` → **bit16 = 1 → 时钟 lane 停在 LP-11 空闲**，且 **bit1 = 0 → D1 连有效 stop state 都没进**
>
> 所以早前写的"clk ✓ · D0 ✓ · D1 ✗"是**错的** —— 实际是**时钟 lane 根本没起来**。
>
> **两台的 CSI host 侧配置一模一样（`N_LANES=0x1` 就是 2 lane，不是故障点）**，差别全在 sensor 有没有按 2 lane 发数据 → **问题 100% 在 sensor 侧，与 host / 驱动 / dtb 无关。**

**两条容易带偏的干扰项**（正常机器上也有，不是故障标志）：
- `hobot-sif: WARNING: Reviced more hsync than expect, check configuration!` ×2（左右目各一条）—— 正常机器也报
- `host1 = not inited` —— 正常现象：4-lane 模式下 host0/host1 并成一组（`change group0 lane_mode to 1 for 4lane`），host1 不单独初始化

#### 根因：sensor 寄存器被写成 1 lane

在 PHY 起来的 3 s 窗口内用 `i2ctransfer` 活体读 sensor 寄存器：

```
样本11-13  shutdownz=0x0 stop=0xc      3018=0x72 3019=0x00 301f=0x00  ← 刚上 mclk，复位默认值
样本14-20  shutdownz=0x1 stop=0x1000d  3018=0x12 3019=0x0e 301f=0x45  ← 驱动写完寄存器表
```

SmartSens 约定：`0x3018[7:5] = lane_num - 1` → `0x12` = **1 lane**（`0x32` 才是 2 lane）；`0x3019` = lane 禁用掩码，`0x0e` = **禁用 D1/D2/D3**。两个寄存器互相印证。

而且 `0x301f`（setting/table ID）**在所有模式下都是 `0x45`** —— 1lane-30fps / 2lane-30fps / 2lane-60fps / 2lane-HDR 全一样 → **这个 sensor 库对所有模式只写同一张 1-lane 寄存器表。**

#### 罪魁嫌疑：`libsc132gs.so.1.0.0` 是旧 OTA 包的残留

| 文件 | 大小 | md5 | 权限 |
|---|---|---|---|
| **正常机（桌面 B）** `/usr/hobot/lib/sensor/libsc132gs.so.1.0.0` | 🟢 **55848** | 🟢 **`357bfde5…`** | 🟢 **`0775`** |
| **64GB 那台**（实际被加载）| 🔴 **46768**（少 9080 B）| 🔴 **`f171ab12…`** | 🔴 **`0644`** |
| git blob `a02f0cd9`（**2026-04-24 "initial packet"**）| 46768 | `f171ab12…` | — |
| `/userdata/hobot/opt/hobot/deps/libsc132gs.so.1.0.0` | 24344 | `f5ba6e4b…` | `rwxr-xr-x` |

**⬤ 已用正常机器验证（桌面 B，`soc_uid ...0a164b9402a4408a00120040`，三路相机全正常）**：

1. **两台 `/etc/version` 完全相同**（`LNX6.1.83_PL5.2_V1.1.0_20260717-1802`）、`softwareversion` 都是 2.1.2、`user_params.json` 里 `stereo_sensor_name` 都是 `sc132gs-1088x1280-20fps-2lane` —— **但 `libsc132gs.so` 二进制不同** → 必有一台不是这个包的原件。
2. 正常机该文件的 **Birth 时间 `2026-07-17 18:02:49` 精确等于版本串 `20260717-1802`** → **正常机的是 20260717 固件包烧出来的原件**。
3. 64GB 那台权限是 `0644`（umask 默认值），而正常机 `/usr/hobot/lib/sensor/` 下**所有** `.so` 都是 `0775` → **典型的"手工 `cp`/`tar` 未带 `-p`"痕迹**，符合改装时 `/usr/hobot`（`mmcblk0p13`）从旧镜像 dump 恢复的推断。
4. `/userdata/hobot/opt/hobot/deps/` 那个 24344 B 的副本**两台完全相同，而且根本不会被加载** —— `/proc/<pid>/maps` 证实 `insight_full` mmap 的是 `/usr/hobot/lib/sensor/` 那个（inode 1228, 55848 B），且 `ros2_env.conf` 的 `LD_LIBRARY_PATH` 里 `/usr/hobot/lib/sensor` 排在 deps 之前。**它是无关变量，不要在它上面花时间。**

**决定性证据：从正常机的库里 dump 出了寄存器表**（该 `.so` 没 strip，`.data` 段可按 `(u32 addr, u32 val)` 解出）：

| 表符号 | `0x3018` | `0x3019` | `0x301f` | lane 数 |
|---|---|---|---|---|
| `..._30fps_1lane_setting_slave` | **0x12** | **0x0e** | **0x45** | 1 |
| `..._30fps_2lane_setting_slave` | 0x32 | 0x0e | 0x45 | 2 |
| `..._60fps_2lane_setting_slave` | **0x22** | **0x0c** | **0x9c** | 2 |
| `..._60fps_setting_master` | **0x12** | **0x0e** | **0x45** | 1 |
| `..._60fps_setting_master_2lane` | 0x22 | 0x0c | 0x9c | 2 |

`0x3018[7:5] = lane_num − 1`（`0x12`→1 lane，`0x22`/`0x32`→2 lane）· `0x3019` = D-lane 禁用掩码（`0x0e`=只留 D0，`0x0c`=留 D0+D1）

- **正常机实测**：`0x3018=0x22`、`0x3019=0x0c`、`0x301f=0x9c` → **精确命中 `_60fps_2lane_setting_slave`** ✅
- **64GB 实测**：`0x3018=0x12`、`0x3019=0x0e` → **精确命中 1-lane 表** 🔴

顺带确认（正常机）：chip id `0x3107/0x3108 = 0x0132` → SC132GS；`0x3222=0x02` + `0x3217/0x3218=0x00` 独属于 `_setting_slave` 表 → **两颗 sensor 都在外触发从模式**（见 § 2.2 末的同步机制）。i2c 总线分布：**左目 = bus 4 @0x32（mipi2）· 右目 = bus 0 @0x32（mipi3）· imx415 = bus 4 @0x1a**。

**结论：「64GB 那台的 sensor 库是旧版残留」—— 强烈证实。** 还差最后一步闭环（三条只读命令，在 64GB 那台上跑）：

```bash
strings /usr/hobot/lib/sensor/libsc132gs.so.1.0.0 | grep -c 2lane   # 得 0 → 100% 定案
i2ctransfer -y -f 4 w2@0x32 0x30 0x1f r1                            # 读表 ID：0x45=1lane 表 / 0x9c=2lane 表
scp root@<64GB>:/usr/hobot/lib/sensor/libsc132gs.so.1.0.0 /tmp/     # 拿回来做符号级 diff
```

🟢 **修复所需的正确库已经捞到 PC** —— `/home/dm/looper/looper_device_files/firmware_ref_from_working_device/libsc132gs.so.1.0.0`（55848 B，`357bfde5…`）。
⚠️ **未安装到任何机器** —— 覆盖固件文件需要你授权，而且更稳妥的做法是让 Looper 给正式的包。

- 实际加载的那个**与 2026-04 旧 OTA 包里的完全一致**，而 **OTA 2.1.2（2026-07-17）把这个文件从 payload 里删掉了**（`Bin 46768 -> 0 bytes`，`sc132gs_tuning.json` 也一起删了）
- 而 `/userdata/postinst` 只做 `cp -a "$INSTALL_DIR/usr/hobot/"* /usr/hobot/`，**从不删文件** → 旧库永远留着
- 它也不是 BSP 原厂文件：同目录其他 sensor 库全是 `May 21 2026` + `rwxrwxr-x`，只有它是 `rw-r--r--`
- 🔴 **加载路径无法用 `LD_LIBRARY_PATH` 绕过** —— `/usr/hobot/lib/libcamdev_manager.so` **硬编码了 `/usr/hobot/lib/sensor`**，所以 deps 里那个不同的 build 根本轮不到
- ⚠️ **"deps 里那个 build 就是正确的 2-lane 版本"是假设，未实测**（override 因硬编码路径没生效）

**附带可疑点**：本次启动期间似乎跑过一次 OTA 安装 —— `/userdata/install/*`、`/userdata/hobot`、`/usr/hobot/lib/sensor/*` 全是本次启动的 mtime。同时 `/userdata/ota_packet/setting/` 里躺着 `update_v2_1_6cmsibv830.2.bundle`（名字像 2.1.6）+ `downloadfinish`，但已装版本写的是 **2.1.2**。**坏 RTC 很可能让 OTA 的版本/时间判断失效，每次开机重装并把旧 sensor 库刷回去。**

#### 根因排序

| # | 原因 | 判断 |
|---|---|---|
| 1️⃣ | **sensor 驱动库版本错配**（旧 OTA 包残留的 1-lane-only 库）| 🔴 **最可能**，证据强。但"换库能修"这一步仍是推断 |
| 2️⃣ | 这台的立体模组本来就是 **1-lane 变体**，而配置写的是 2-lane | 🟡 可能。**区分方法**：拿一台正常 insight9 对比 `libsc132gs.so.1.0.0` 的 md5 和 `user_params.json`。顺带：1 lane @1200 Mbps 实测能跑 1088×1280 ~60 fps，**对 20 fps 双目绰绰有余** |
| 3️⃣ | ~~MIPI 排线在改装时被弄坏~~ | ❌ **已排除**：两条**物理独立**的链路（不同接插件、不同 i2c 总线）出现**逐位相同**的失败；lane0+clk 在 1200 Mbps 下跑满帧且所有错误计数器为 0。物理损伤解释不了这种对称性 |
| 4️⃣ | ~~SoC CSI 控制器 / DPHY / 供电 / 过温~~ | ❌ **已排除**：无 regulator/undervoltage/thermal 日志；48 °C；CPU/BPU/DDR 全频满速；USB SuperSpeed self-powered；所有 MIPI 时钟 `get == set` |
| 5️⃣ | ~~模式配置 / PHY 卡死~~ | ❌ 早前已排除 |

#### 这台机器改装前出过图（证据 + 保留意见）

- `/app/calibration/looper.calibration.json`（md5 `bbad854d…`，fx 330.015/331.049）**与镜像自带默认值不同**（`/userdata/install/share/example/config/looper.calibration.json`，md5 `e59f213d…`，fx 328.829/332.417）→ 不是出厂占位文件
- `/app/calibration/rgb.calibration.json` 在整个文件系统里**只有这一份**（镜像里没有默认版本），内容含 RGB 相对 `CAMERA_LEFT` 的外参 → **做过真实的双目 + RGB 联合标定，三颗相机都出过图**
- 日期 **Jul 17 2026**，与固件 2.1.2 OTA、内核/uboot 构建同日
- ⚠️ **保留意见**：无法 100% 排除标定文件是从别的机器拷来的

#### RTC 坏掉正在持续销毁证据

`hwclock -r` 返回 1970，系统时间每次启动都恢复到 **2025-08-26 23:23** →
- `/userdata/insight_full_crash_YYYYMMDD_HHMMSS.log` **文件名每次都撞车、被反复覆盖**（现存 22 个全是本次启动的）
- `journalctl --list-boots` 只有 boot 0；无持久化 journal；`/app/startup.sh` 每次开机还 `rm -rf /var/log/*`
- `/userdata/log/coredump/` 和各 `archive/` 目录全空
- **很可能让 OTA 每次开机重装旧包**

**→ 修 RTC 纽扣电池应该排在很前面**，它同时是"证据被毁"和"OTA 反复重装"两个问题的源头。

#### RGB（imx415）仍未验证

- `insight_full` 的 backtrace 是 `initCameras(StereoCameraConfig&, RgbCameraConfig&)`，两者都配了，但**死在第一路立体相机上，RGB 根本没走到**（`/sys/class/vps/mipi_phy/status/host` 显示只有 host2 有初始化痕迹，host0/1/3 全是 `--------`）
- 板上所有 sample 工具的 sensor 列表里**只有 sc132gs 和 sc835hai，没有 imx415**，无法单独拉起 rx0
- `insight_full_uvc` 理论上能测，但它会重配 USB gadget、**会把 NCM SSH 链路踢掉** —— 要做得先准备串口
- 可推断（非实测）：RGB 用的 `libimx415.so` 是**未被覆盖的原厂 BSP 版本**（May 21 2026），且在独立的 DPHY group 0 上，不受 sc132gs 库问题影响

#### 板级信息

`board_id: 0x0731` · `/proc/device-tree/model` = `D-Robotics X5 EVB LP4 DEMI board` · `/app/ProductName` = `insight9` · `/app/SerialName` = `imx415`（RGB 变体选择器，不是序列号）· 软件版本 `2.1.2` · `/userdata/install/commit_hash` = `647feca9…` · `uname`: `6.1.83-DR-PL5.2_V1.1.0 #1 SMP PREEMPT Thu Jul 16 14:16:57 CST 2026 aarch64`

#### 诊断入口速查（以后排查相机问题直接用）

```bash
/sys/class/vps/mipi_host{0..3}/status/{regs,cfg,icnt,clock,info,snrclk}   # PHY 寄存器 + 错误计数
/sys/class/vps/mipi_phy/status/host                                       # 各 host 的 PHY 参数
/sys/kernel/debug/sif{0..3}/fps   /sys/kernel/debug/isp/fps               # 实时帧率
/app/platform_samples/sample_isp/multi_isp_vflow/multi_isp_vflow -s N     # 同时测 mipi2+mipi3
/app/platform_samples/sample_vin/get_vin_data/get_vin_data
insight_param status                                                      # 需 insight_full 活着（unix socket 客户端）
```

### 2.3 硬件余量

| 硬件 | 总量 | 固件占用 | 我们可用 |
|---|---|---|---|
| CPU | 8× Cortex-A55 @1.5 GHz | ⬤ 1.7 核（静止）/ 3.5 核（运动）| ⬤ **4.5 核**（VIO 开）/ **6.6 核**（VIO 关）|
| **内存** | ⬤ **物理 DRAM 3.9 GiB**，其中 **~2.5 GiB 被 ion 预留** → `MemTotal` 1307 MB，**无 swap** | ⬤ 212 MB | 🔴 ⬤ **~911 MB** ← **最硬的约束** |
| BPU | 1× Bayes-e，10 TOPS | ⬤ 87–95% 时间 | 🔴 ~5–13%，且**与 CPU 共用 DDR** |
| 存储 `/userdata` | 9.2 G / **53 G**（64GB 版）| 6.2–6.5 G | 2.3 G / 🟢 **44 G** |

#### ⭐ 内存瓶颈可能不是硬件限制 —— 2.5 GiB 被 ion 预留了

⬤ 实测：**这颗 X5 的物理 DRAM 是 3.9 GiB，其中约 2.5 GiB 在启动时被 ion（媒体/BPU 缓冲池）静态预留**，剩下的才是 Linux 的 `MemTotal 1338644 kB ≈ 1307 MB`。

**这意味着"1307 MB 无 swap"不是芯片的物理上限，而是一个可调的分配决策。** 如果能把 ion 预留砍下来一部分（DT / bootargs 层面的改动），CPU 可用内存能多出几百 MB —— **这会直接改变所有方案的内存判断**：

| 若 ion 预留减少 | `MemTotal` | 影响 |
|---|---|---|
| 现状（~2.5 GiB ion）| 1307 MB / 可用 ~911 MB | 🔴 DINOv2 504 MB 就贴到上限；曾实际 OOM 并杀到固件的 `imu_pub` |
| ○ −512 MB | ○ ~1.8 GB / 可用 ~1.4 GB | 🟢 方案 6 的 480 MB 变得很宽裕 |
| ○ −1 GB | ○ ~2.3 GB / 可用 ~1.9 GB | 🟢 内存不再是约束，方案选择完全由用时决定 |

⚠️ **代价与风险（未验证）**：ion 池是相机流水线（VPF 缓冲、ISP、深度推理、BPU 张量）在用的。砍太多会让固件申请缓冲失败 —— 表现可能就是各种 `CreateAndRunVflow` 类的启动失败。所以这件事**必须 Looper 那边评估，不能自己硬改**。

> 🔴 **这应该加进 LooperHub 索取清单，而且优先级很高** —— 内存是全项目最硬的约束，而这是唯一一个能从根上放宽它的手段。相比之下"关 VIO"只省 2 个 CPU 核、"depth 降帧率"只释放 BPU，都不解决内存。
> 要问清的具体问题：① 当前 ion 预留 ~2.5 GiB 的依据是什么、实际水位多少 ② 能否降到 1.5–2 GiB ③ 有没有运行时可查的 ion 使用量（这样能实测水位而不是猜）。

### 2.4 CPU 算力基准（自造 onnx 模型实测，6 线程）

为判断"fp16 是否是瓶颈"专门造了对比模型：

| 算子 | fp16 | fp32 | 结论 |
|---|---|---|---|
| Conv | ⬤ **654 ms** | ⬤ 1631 ms | 🟢 **fp16 快 2.5 倍** —— A55 有 ARMv8.2 FP16 硬件支持 |
| GEMM | ⬤ 181 ms | ⬤ **126 ms** | fp32 快 1.4 倍（仅 attention 类算子有小幅收益）|

**算力上限**：6 线程 fp16 Conv ~**39 GFLOPS**、fp32 GEMM ~**17 GFLOPS**。

> **结论：fp16 不是瓶颈，不要浪费时间转 fp32。** 这条推翻了早期假设。

---

## 3. VIO 负载

`insight_full` 线程级 CPU 采样（差分采样 `/proc/<pid>/task/*/stat`，2 s 间隔）：

| 状态 | VIO 相关合计 | 深度/立体 | RGB/JPEG | 固件总计 |
|---|---|---|---|---|
| **静止** | ⬤ **0.92 核** | ⬤ ~0.64 核 | ⬤ ~0.14 核 | ⬤ ~1.7 核 |
| **运动**（手持晃动）| ⬤ **2.11 核** | ⬤ ~0.64 核 | ⬤ ~0.14 核 | ⬤ ~3.5 核 |

**VIO 运动时是静止时的 2.3 倍。** 静止时低是因为 **ZUPT**（零速更新）—— 检测到静止就跳过大部分前端计算，所以早期"VIO 只占 1 核"的观察是静止状态的假象。

静止状态的线程拆分（`ps -L -o tid,pcpu,comm`）：`vio_frontend`×2 = 67.7% · `imu_cap`×2 = 15.3% · `vio_backend` 6.6% · `imu_pub` 3.6% · `vio_stereo` 2.1% · `vio_imu` 1.7% · `pose_pub` 1.5%。

**关掉 VIO 的收益**：可用 CPU 从 **4.5 核 → 6.6 核**（+47%）。
🔴 **但 `libinsight_full_plugin.so` 里搜不到任何 enable/disable VIO 的字符串，`/app/looper.vio.runtime.json` 只有 MSCKF 算法参数** —— 需要 Looper 提供编译选项或 LooperHub 接口。

---

## 4. BPU 分析

### 4.1 占用与帧率

⬤ `hrut_somstatus` 显示 BPU `ratio` 在固件运行时稳定 **87–95%**（早期读到 100 是瞬时值）。这是**时间占用率**，不等于算力利用率 —— 其中含大量内存等待，所以"BPU 有水分"的判断是对的。

🔴 **但帧率降不下来**（⬤ 实测）：

- `ros2 param dump /insight_full` **没有任何帧率参数**（只有 `perception_engine: depth`、`enable_hand: false`、`stereo_sensor_name: sc132gs-1088x1280-20fps-2lane`）
- 可用 sensor mode 只有 **20 / 30 / 40 / 50 / 60 fps**，最低就是 20
- 所以 **"depth 降到 5 fps 释放 5.5 TOPS" 这条路堵住了**，除非 Looper 改固件

⚠️ 板上另有一个更小的深度模型 `DStereoV2.5_int16_544_448.bin`（当前用 `DStereo.bin`）。**未替换 —— 动固件文件需要授权。**

### 4.2 算子构成 —— 三个模型能否上 BPU

用 `onnx` 读取模型图统计算子分布（这是**代码事实 + 分析判断**，最终需 OpenExplorer 编译验证）：

| 模型 | 节点数 | 算子种类 | BPU 友好算子 | **BPU 不友好算子** |
|---|---|---|---|---|
| **SuperPoint** | 237 | 31 | Conv×12、MaxPool×8、Relu×10 | **GridSample×1**、TopK×1、ScatterND×4、Where×9、Range×12、ConstantOfShape×7、Mod×1 |
| **LightGlue** | 1598 | **43** | MatMul×96 | **Einsum×36**、**IsInf×40**、**IsNaN×20**、Where×80、And×44、Greater×21、Less×20、ArgMax×2、GatherElements×1 |
| **DINOv2-base** | 951 | **23** | MatMul×96、Softmax×12、ReduceMean×50、Erf×12、Conv×1 | Where×1（几乎没有控制流）|

| 模型 | 判断 | 理由 |
|---|---|---|
| **DINOv2** | 🟢 **算子最干净** | 23 种算子、标准 ViT、无 Einsum、无数值检查、几乎无控制流。三个里最适合上 BPU |
| **SuperPoint** | 🟡 **可切图部署** | backbone（Conv/MaxPool/Relu）上 BPU，后处理（TopK / GridSample / ScatterND / NMS）留 CPU。计算量绝大部分在 backbone，收益大，但后处理的 CPU 耗时未知，需拆分实测 |
| **LightGlue** | 🔴 **风险最高** | 36 个 Einsum + 60 个 IsInf/IsNaN + 大量 Where/And 掩码逻辑，典型的 BPU 不支持算子。即使能编译也会被切成很多段，BPU↔CPU 来回拷贝可能让性能倒退 |

⚠️ **判断能否上 BPU 的标准不是内存**（BPU 与 CPU 共用同一块 1307 MB DDR，没有独立显存），而是：① 算子能否被 `hb_mapper` 编译 ② 子图切分程度 ③ Bayes-e 主跑 int8，需量化+校准 ④ DDR 总量装得下（和 CPU 推理同一个约束）。

### 4.3 释放算力值多少

| 情景 | 重定位延迟 | 收益 |
|---|---|---|
| 现状（全 CPU，top_k=3 串行）| ⬤ 15.1 s | — |
| 只有 SuperPoint backbone 上 BPU（**仍用 LightGlue**）| ○ ~14.3 s | 🔴 **改善 5%，无意义**（LightGlue 那 8.6 s 没动）|
| 只有 DINOv2 上 BPU（**仍用 LightGlue**）| ○ ~10 s | 🔴 无意义，同上 |
| **DINOv2 上 BPU + LightGlue 换 BF 匹配**（方案 6）| ○ **~1.5 s** | 🟢 **快 10 倍，且检索质量已知** |

> **早期结论"BPU 的价值 100% 取决于 LightGlue 能否上"已被推翻。** 正确的结论是：**不要试图加速 LightGlue，而是不用它** —— cv2 BF 匹配 38.6 ms 就能替代它的 2870 ms（§ 5.5）。删掉 LightGlue 之后，BPU 的价值转移到 DINOv2 身上，而 DINOv2 恰恰是三个模型里最容易上 BPU 的。**这是整份文档最关键的一次判断修正。**

---

## 5. 算法实测

环境：ONNXRuntime 1.18.0（板载自带）· CPU EP · fp16 模型 · opencv-python-headless 4.6.0.66

### 5.1 单模型延迟

| 模型 | 分辨率 / 规格 | 线程 | 延迟 | 峰值 RSS |
|---|---|---|---|---|
| **SuperPoint** | **320×272**（Looper 实际输入）| 6 | ⬤ **1010 ms** | ⬤ 145 MB |
| SuperPoint | 480×848（早期误用）| 6 | ⬤ 4772 ms | — |
| **LightGlue** | 512 kpts 固定 | 6 | ⬤ **2870 ms** | ⬤ 169 MB |
| LightGlue | 同上 | 1 | ⬤ 5900 ms | — |
| LightGlue | 同上 | 3 | ⬤ **3151 ms**（1.87×）| — |
| **DINOv2-base** | 224×224 | 6 | ⬤ **5190 ms** | ⬤ 816 MB → 504 MB |

**分辨率链路（已核对代码，1010 ms 这个数是对的）**：

| 环节 | 尺寸 | 依据 |
|---|---|---|
| Looper 发布的 `infra1/image_rect_raw` | ⬤ **544×640 `mono8`**（depth 同尺寸 `mono16`）| 实机抓帧的 topic meta：`w=544 h=640 enc=mono8 step=544 len=348160` |
| `looper_bridge_node.py` | **不缩放**，原样透传 | 全文无 `cv2.resize` |
| **送进 SuperPoint 前** | 🔑 **缩到 engine 输入尺寸 = 320×272** | `models_trt.py:160` `image = cv2.resize(input_image, (w_net, h_net))`，之后 `models_trt.py:170-171` 按 `scale_x/scale_y` 把关键点坐标还原回原图；代码注释明确写着 "so Looper (640x544) and other resolutions match" |
| engine 构建尺寸 | `optShapes 1x1x320x424` / `minShapes 1x1x240x272` | `tinynav/models/Makefile`，注释 "RealSense 240x424 + Looper 320x272" |

> 所以 **main 的流水线本身就有一次 2× 降采样**（544×640 → 320×272），我按 320×272 测的 1010 ms 与线上行为一致。早期按 480×848 测出的 4772 ms 是**错的**。
>
> 💡 **这是一个可调的速度-精度旋钮**：跑全分辨率 640×544 是 4× 像素 → SuperPoint 约 **4000 ms**，**直接超 5 秒预算**，不可行。但 **ORB 跑全分辨率只要约 186 ms**（46.5 ms × 4），完全负担得起 —— **对方案 1 来说这是一个几乎免费的召回率提升项**，值得在 T-2 的评测里一并对比。
> LightGlue 线程扩展在 **3 线程后饱和**（1t→3t 提速 1.87×，3t→6t 几乎不动）。

### 5.2 DINOv2 内存四档 —— 换运行时 ≫ 换精度

```
arena=ON   load=195MB  peak=816MB  lat=5271ms
arena=OFF  load=185MB  peak=504MB  lat=5237ms   ← 省 312MB，延迟不变
```

设 `sess_options.enable_cpu_mem_arena=False` + `enable_mem_pattern=False` 即可。这两个开关是 ORT 的"用内存换速度"优化（arena 预申请大块内存且**不归还系统**；mem_pattern 预分配峰值大小的连续块），**在内存紧张的嵌入式设备上应该关掉**。实测延迟几乎不变 —— **省 312 MB 是纯赚**。同样适用于 SuperPoint / LightGlue。

| 配置 | 权重 | 峰值 RSS | 省了什么 | 检索质量影响 |
|---|---|---|---|---|
| ORT fp16 + arena on（原状）| 195 MB | ⬤ **816 MB** | — | 基准 |
| **ORT fp16 + 关 arena** | 185 MB | ⬤ **504 MB** | −312 MB：arena 预留 | **无损** |
| ORT **int8** + 关 arena | ○ 98 MB | ○ **~420 MB** | 再 −84 MB：**只压权重，activation 一分没省** | 轻微下降 |
| **hbDNN int8 on BPU** | ○ 86 MB | ○ **~150–200 MB** | 再 −220 MB：**ORT 的 activation 膨胀整个消失** | 待验 |
| dinov2-**small** fp16 + 关 arena | ○ ~49 MB | ○ **~180 MB** | 延迟也降到 ○ ~1300 ms | 中等下降（几个百分点）|

> **关键区分**：504 MB 里只有 185 MB 是权重，另外 319 MB 是 ORT 的 activation + 中间张量。所以 **ORT 里 int8 最多省 84 MB**；真正的大跳是**换运行时**（ORT → hbDNN，缓冲编译期定死）。**"int8 降内存"这句话在 ORT 语境下几乎不成立，只有配合上 BPU 才兑现。**
> ⚠️ hbDNN 那一档是估算（权重 86 MB + ViT-B 257 tokens 的 activation 工作集 ~50 MB），编出 `.bin` 后必须实测复核。

**OOM 实录**：早期一次直接被 OOM killer 杀死 ——
```
imu_pub invoked oom-killer
Out of memory: Killed process 1477 (python3) total-vm:1656976kB, anon-rss:931176kB
```
⚠️ 触发 oom-killer 的是 `insight_full` 的 `imu_pub` 线程 —— **DINOv2 的内存压力已经威胁到相机固件本身**。

### 5.3 ORB 路线（8 线程）

| 操作 | 延迟 | 备注 |
|---|---|---|
| ORB `detectAndCompute` @320×272，nfeat=1024 | ⬤ **46.5 ms** | 实得 978 keypoints |
| BF-Hamming crossCheck（984×974 desc）| ⬤ **26.0 ms** | 283 matches |
| `findFundamentalMat` RANSAC | ⬤ **87.1 ms** | ⚠️ 合成数据 worst case |
| `solvePnPRansac`（283 pts）| ⬤ **2.6 ms** | |
| **peak RSS** | ⬤ **47 MB** | |

> RANSAC 那 87 ms 是保守值：测试图是随机合成的，283 个匹配绝大多数是外点，RANSAC 迭代到上限。真实场景内点率高，通常快 3–5 倍。

### 5.4 LightGlue 多实例并行

重定位里 top_k 次 LightGlue **无数据依赖**（都只依赖 query 特征 + 各自的参考帧），可以真并行（ORT `run()` 释放 GIL）：

```
SuperPoint(query) ─┬─→ LightGlue(query, ref1) ─┐
                   │   LightGlue(query, ref2) ─┼─→ 合并 → PnP
检索 top-k ────────┴─→ LightGlue(query, ref3) ─┘
      串行                这 3 个可并行
```

| 配置 | 串行 3 次 | **并行 3 次** | 加速比 |
|---|---|---|---|
| 每实例 1 线程（占 3 核）| ⬤ 18201 ms | ⬤ **6606 ms** | 2.76× |
| 每实例 2 线程（占 6 核）| ⬤ 12027 ms | ⬤ **5399 ms** | 2.23× |

**为什么并行 3 个 2 线程实例（5399 ms）比单个 2 线程实例（3875 ms）慢 1.39 倍？** 6 个工作线程超过了可用核数（运动时只有 4.5 核空闲），加上内存带宽和 cache 争抢 —— **并行不是免费的**。

### 5.5 ⭐ 用传统匹配替代 LightGlue

**LightGlue 既是 CPU 上最慢的（2870 ms，占总耗时 57%），又是最难上 BPU 的（Einsum×36 + IsInf/IsNaN×60）。那就不用它** —— 保留 SuperPoint 的强描述子，改用传统 BF 匹配。

实测 **512×256 float32** 描述子的匹配耗时（就是 SuperPoint 的输出规格）：

| 匹配方式 | 延迟 | 备注 |
|---|---|---|
| numpy 余弦 + mutual-NN | ⬤ 264.8 ms | numpy 用未优化 BLAS，慢 |
| numpy 余弦 + Lowe ratio | ⬤ 259.5 ms | 同上 |
| **cv2 BFMatcher L2 + crossCheck** | ⬤ **38.6 ms** | ✅ 推荐（更严格）|
| **cv2 knnMatch k=2 + ratio test** | ⬤ **20.6 ms** | ✅ 更快 |
| *（对照）LightGlue* | *⬤ 2870 ms* | **慢 74–140 倍** |

> ⚠️ 匹配**速度**已实测，但这个组合的**recall 完全没有数据** —— 见 § 10.2 的数据空白。

---

## 6. 六个算法总表

重定位是**三层流水线**（全局检索 → 局部特征 → 匹配），每层都有「学习式」和「经典」两种选择。SuperPoint 分辨率统一 **320×272**，描述子规格统一 **512×256 float32**。

### 6.1 总表

| 层 | 算法 | 类型 | **CPU 用时** | **峰值 RSS** | **地图/帧** | **BPU 可行性** |
|---|---|---|---|---|---|---|
| **① 全局检索**<br>几千帧里选 top-k | DINOv2-base (CLS) | 学习 | ⬤ **5190 ms** | ⬤ 816 MB → **504 MB** | 3 KB | 🟢 **算子最干净** |
| | DINOv2 patch VLAD | 学习 | ○ >5190 ms | ○ >816 MB | ○ ~16 KB | 🟢 同上，输出改吐 patch tokens（786 KB）|
| | SuperPoint BoW | 经典 | ○ ~5 ms | ○ ~5 MB | 2 KB | 不需要（cv2+numpy）|
| | DBoW3 + ORBvoc | 经典 | ○ ~5 ms | ○ **145 MB**（词典常驻）| ○ 1.5 KB | 不需要 |
| **② 局部特征**<br>提关键点+描述子 | SuperPoint @320×272 | 学习 | ⬤ **1010 ms** | ⬤ 145 MB | ⬤ **528 KB** | 🟡 **可切图** |
| | ORB (nfeat=1024) | 经典 | ⬤ **46.5 ms** | ⬤ ~10 MB | ⬤ **39 KB** | 不能（非神经网络）|
| **③ 匹配**<br>每次配对 | LightGlue | 学习 | ⬤ **2870 ms** | ⬤ 169 MB | — | 🔴 **最难** |
| | BF-L2 + crossCheck（SP 描述子）| 经典 | ⬤ **38.6 ms** | ⬤ ~5 MB | — | 不需要 |
| | BF-L2 knn + ratio（SP 描述子）| 经典 | ⬤ **20.6 ms** | ⬤ ~5 MB | — | 不需要 |
| | BF-Hamming（ORB 描述子）| 经典 | ⬤ **26.0 ms** | ⬤ ~5 MB | — | 不需要 |

**三层共用的后处理**（与选哪个算法无关）：`findFundamentalMat` RANSAC ⬤ **87.1 ms** · `solvePnPRansac` ⬤ **2.6 ms**

### 6.2 recall 与用时的错位

> **影响 recall 最大的那层用时可以很小；用时最大的那层对 recall 只是中等影响。这个错位是所有优化的依据。**

| 层 | 作用 | **对 recall 的影响** | **对用时的影响** |
|---|---|---|---|
| **① 全局检索** | 决定 top-k 里有没有正确帧 | 🔴 **决定性 —— 第一道门**，没命中后面全白搭 | 🟢 极小（BoW 5 ms）<br>🔴 或极大（DINO 5.19 s，占 34%）|
| **② 局部特征** | 描述子质量决定能匹配上多少点 | 🟡 中等 | 🟡 中（SP 1010 ms / ORB 46 ms，**22 倍差**）|
| **③ 匹配** | **第二道门**：需 ≥50 匹配点、≥80 landmark、PnP inliers ≥50（`map_node.py`）| 🟡 中等 | 🔴 **最大（LG 8610 ms = 总耗时 57%）** |
| top_k | 第一道门的宽度 | 🔴 直接决定（R@1 88.87% → R@10 99.25%）| 🔴 匹配层的乘数（top_k=3 就是 ×3）|

由此得出三条优化逻辑：

1. **检索层要保质量，但它本不该贵** —— 唯一贵的是 DINOv2 走 CPU。挪到 BPU（int8）就同时拿到「质量已知」和「几乎不花时间」。
2. **匹配层是用时的绝对大头（57%），但对 recall 只是中等影响** → **这才是最该动刀的地方**。LightGlue → BF-L2：8610 → 116 ms（**省 98.6%**），描述子仍是 SuperPoint 的。
3. **局部特征是唯一真正的速度-精度权衡点**（SP 强得多但贵 22 倍）。建议先留 SP，把 1010 ms 当可接受成本。

---

## 7. 九个方案对比

### 7.1 九配置总表

统一口径：**top_k=3**（除方案 2）· 用时含 **RANSAC×3 (261 ms) + PnP (3 ms)** · 内存含 **Python/ROS 基础 ~150 MB**

| # | 方案 | 检索 | 局部特征 | 匹配 | **单次用时**<br>(6 核) | **4.5 核**<br>(VIO 开) | **内存** | **地图** | **recall 确定性** | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|
| **0** | **现状 main 全 CPU** | DINO 5190 ms | SP 1010 ms | LG 8610 ms | ⬤ **15.1 s** | ○ ~20 s | ⬤ ~920 MB 🔴 | 9.5 GB 🔴 | 最高（已知）| 🔴 跑不动 |
| **1** | **ORB 全经典** | DBoW3 5 ms | ORB 47 ms | BF-H 78 ms | ⬤ **0.39 s** | ○ ~0.5 s | ⬤ ~310 MB | ~720 MB | 🔴 最低**且未知** | ⭐ **第一版** |
| **2** | 学习式 CPU 极限<br>(top_k=1, 3 线程)| BoW 5 ms | SP | LG | ⬤ **4.5 s** | ○ ~6 s | ⬤ ~420 MB | 9.5 GB 🔴 | 高（近 main）| 🔴 超预算 |
| **3** | ~~LightGlue 上 BPU~~ | BoW | SP@BPU | **LG@BPU** | ○ ~0.4 s | ○ ~0.4 s | ~420 MB | 9.5 GB 🔴 | 高 | ❌ **放弃** |
| **4** | 混合兜底 | DBoW3 + BoW | ORB + SP | BF-H + LG | 常态 0.39 s<br>复核 4.5 s | ○ 0.5 / 6 s | ○ ~575 MB | ~10 GB 🔴 | 高 | 🟡 复杂度失控 |
| **5a** | **BoW + SP + BF** | BoW 5 ms | SP 1010 ms | **BF-L2 116 ms** | ⬤ **1.40 s** | ○ **~1.8 s** | ○ ~280 MB | 需瘦身 | 🟡 **检索未知** | ✅ **退路** |
| **5b** | BoW + SP@BPU + BF | BoW 5 ms | ○ SP@BPU ~50 ms | BF-L2 116 ms | ○ **0.44 s** | ○ ~0.5 s | ○ ~280 MB | 需瘦身 | 🟡 同上 | 🟢 优化项 |
| **6** | **DINO@BPU + SP + BF** | ○ DINO@BPU ~150 ms | SP 1010 ms | **BF-L2 116 ms** | ○ **1.54 s** | ○ **~1.9 s** | ○ ~430–480 MB | 需瘦身 | 🟢 **检索已知**<br>（待验 int8）| ⭐ **长期目标** |
| **6+** | DINO@BPU + SP@BPU + BF | ○ DINO@BPU ~150 ms | ○ SP@BPU ~50 ms | BF-L2 116 ms | ○ **0.58 s** | ○ ~0.6 s | ○ ~430 MB | 需瘦身 | 🟢 同上 | 🟢 Phase 4 |

### 7.2 PR #136 是哪个方案

**PR #136（`junlinp/add-orb-matcher-trt-tests`）实现的就是方案 1** —— 也是本项目的代码基线。代码事实（已核对 `x5/wheel-nav` 分支）：

| 层 | PR #136 用的 | 代码位置 |
|---|---|---|
| 全局检索 | **DBoW3 + 预训练 ORBvoc**（145 MB，Git LFS）| `models_trt.py:352` `class DBoW3Engine` → `import pydbow3` |
| 局部特征 | **ORB** | `models_trt.py:197` `class ORBFeatureTRTCompatible` |
| 匹配 | **BF-Hamming + RANSAC** | `models_trt.py:242` `class ORBMatcher` |
| 模式开关 | `--loop-closure-mode {embedding,bow}` / `--loop-closure-use-bow` | `build_map_node.py:1149-1161`、`map_node.py` 同构 |
| TRT soft-import | ✅ 有 | `models_trt.py` 顶部 |

设计上很克制：ORB/BF 的输出**刻意做成 SuperPoint/LightGlue 兼容格式**（`ORBFeatureTRTCompatible` 这个名字就是这个意思），而且是**模式开关而非硬替换**，所以换回学习式路线不用改调用方。这是它比 PR #194 更适合做基线的主要原因。

### 7.3 逐方案细节

**方案 5a：SuperPoint + 传统匹配（绕开 LightGlue）**

| 变体 | 检索 | 局部特征 | 匹配×3 | RANSAC×3 | PnP | **合计** | 占核 |
|---|---|---|---|---|---|---|---|
| 5a：纯 CPU | BoW ~5 ms | SP 1010 ms | 3×38.6 = 116 ms | 261 ms | 3 ms | ⬤ **~1.40 s** | 6 |
| 5b：SP 上 BPU | BoW ~5 ms | ○ SP ~50 ms | 116 ms | 261 ms | 3 ms | ○ **~0.44 s** | <1 核 + BPU |

**这正是"一部分放 BPU、一部分放 CPU"的最佳落点**：SuperPoint backbone → BPU（Conv 为主）· SuperPoint 后处理（TopK/GridSample/NMS）→ CPU · 描述子匹配 → CPU（cv2 BF）· 检索 → BoW（CPU ~5 ms）· **LightGlue 整个删掉**。

**方案 6：DINOv2 int8 上 BPU + SuperPoint CPU + BF 匹配**

方案 5a 已经把用时压到 1.4 s，但检索层换成了 recall 没数据的 BoW。方案 6 把检索层换回 DINOv2 —— 用 BPU 承担算力，用 hbDNN 替掉 ORT 的内存膨胀，**把唯一的未知量变成已知量**。

DINOv2-base @224×224、ViT-B/14 → 257 tokens ≈ **22.9 GFLOPs（11.4 GMAC）**。BPU 10 TOPS int8 理想值 2.3 ms；按实效 20–40% + 与深度推理争抢排队 → ○ **50–200 ms**。

| 环节 | 现状（全 CPU）| **方案 6** |
|---|---|---|
| 全局检索 DINOv2 | ⬤ 5190 ms | ○ **50–200 ms** |
| 局部特征 SuperPoint | ⬤ 1010 ms | ⬤ 1010 ms（不动）|
| 匹配 ×3 | ⬤ 8610 ms (LG) | ⬤ **116 ms** |
| RANSAC×3 + PnP | ⬤ 264 ms | ⬤ 264 ms |
| **合计** | **~15.1 s** | ○ **≈ 1.4–1.6 s** |

**方案 6 的六个风险**（按严重程度）：

| # | 风险 | 为什么严重 | 怎么便宜地验证 |
|---|---|---|---|
| 🔴 1 | **BPU 能否被第二个进程使用** | 固件独占 BPU 且占用 87–95%，depth 帧率**已证实不可调**。不通则退方案 5a。还要看抢占后 depth 会不会掉帧（`/slam/depth` 是 planning 的输入，`planning_node.py:371`）| 板上 20 行 ctypes 调 `libdnn.so` 反复推理，同时 `hrut_bpuprofile` 看 depth 的 FC 时间是否恶化。**半天** |
| 🔴 2 | **int8 量化对检索描述子的影响** | ViT 的 PTQ 公认难做（LayerNorm/GELU outlier、attention 动态范围大）。分类掉 1% 无所谓，但我们用 CLS/VLAD 做**余弦排序**，描述子空间轻微畸变就可能让 R@1 掉几个点 | PC 上 `hb_model_infer` 出描述子 → 跑 `map_retrieval_self_consistency.py` → 对比 fp16 基线 78.80% / 88.87% |
| 🔴 3 | **地图端与板端描述子必须同源** | 地图在 PC 上用 TRT fp16 建，板上是 int8 BPU —— 同一张图过两个模型出的描述子不一致，排序质量会**额外**退化 | 建图也改用量化后的同一个模型（`hb_model_infer` 在 x86 上位精确模拟）。意味着离线建图流程要改 |
| 🟡 4 | **SuperPoint 成为新的绝对大头** | 砍掉 LightGlue 后 SP 占 65–70% | 建议**先接受 1.5 s**（§ 8 已论证够用），SP 上 BPU 列为可选优化 |
| 🟡 5 | **SP 描述子 + BF 匹配的 recall 未知** | 方案 5/6 共有，绕不开 | 同风险 2 的 PC 实验，可并行 |
| 🟢 6 | CLS 还是 patch VLAD | VLAD 明显更好（88.87% vs 78.80%），代价是 BPU 输出从 3 KB 变 786 KB + CPU 端 VLAD assignment（几 ms）| 建议用 VLAD；需确认 `tinynav/core/vlad.py` 的聚类中心来源能否搬上板 |

**方案 6 的工作清单**（约 2 周，前 5 项 ≈1 周即可给出"走不走得通"的答案）：

| # | 工作 | 在哪 | 工作量 | 阻塞性 |
|---|---|---|---|---|
| 0 | 验 BPU 并发（见风险 1）| 板上 | 0.5 天 | 🔴 一票否决 |
| 1 | fp16 onnx → fp32，冻结动态维为 `1×3×224×224`（BPU 要静态 shape）| PC | 0.5 天 | 🔴 |
| 2 | 采校准集 ~100 张真实 Looper 图，预处理与推理**完全一致** | 板上采图 | 0.5 天 | 🔴 |
| 3 | `hb_mapper` 编译 int8 `.bin` | PC + OpenExplorer | 1–3 天 | 🔴 |
| 4 | PC 上验 int8 recall（见风险 2）| PC | 1 天 | 🔴 第二个否决项 |
| 5 | PC 上验 SP+BF recall（见风险 5，可并行）| PC | 1–2 天 | 🔴 |
| 6 | 写 hbDNN 的 Python 封装（ctypes/pybind），替换 `Dinov2TRT` | 板上 | 2–3 天 | 🟡 |
| 7 | 建图侧改用量化模型出描述子（见风险 3）| PC | 1 天 | 🟡 |
| 8 | `map_node` 把 LightGlue 换成 `cv2.BFMatcher(NORM_L2)` + ratio test，调门槛 | PC 先跑通 | 1–2 天 | 🟡 |
| 9 | 输入布局决策：归一化放进模型（加 Mul/Sub 节点）还是 CPU 预处理 | PC | 0.5 天 | 🟢 |
| 10 | 可选：SuperPoint 切图上 BPU（→ 方案 6+）| PC + 板上 | 3–5 天 | 🟢 |

### 7.4 上板难度

| | 方案 1 ORB + DBoW3 | 方案 5a BoW + SP + BF |
|---|---|---|
| 检索层实现 | ✅ PR #136 自带 | ✅ **PR #194 的 `bow_retrieval.py`**（148 行，只 import `cv2`+`numpy`）|
| 检索层依赖 | 🔴 **`pydbow3` 不在 PyPI**，aarch64 上可能要编 DBoW3 + pybind + OpenCV C++ 头 | 🟢 **纯 Python，零新依赖** |
| 特征层依赖 | cv2（装 headless **4.6.0.66**；4.8 会挂，需 numpy≥1.22）| onnxruntime 1.18（**板上自带**）+ cv2 |
| 词典 | ORBvoc.txt **145 MB**（Git LFS）| 自训，几 MB |
| 集成工作量 | 🟢 小 —— #136 就是完整的方案 1 | 🟡 中 —— 要把 #194 的两个文件缝进 #136 的模式开关框架 |
| 单次用时 | 0.39 s | 1.40 s |

> **判定实验（半小时）：在 X5 上试装 `pydbow3`。** 装得上 → 直接用 #136 跑方案 1；装不上 → 把 #194 的 `bow_retrieval.py` 接进来走方案 5a。**两条路都有现成代码，不会卡死。**

---

## 8. 时延预算（5 秒）

**已确定的预算：5 秒/次。** 同事实测 Jetson 版 tinynav 约 **3 s/次**，轮速机器人可放宽到 5 s。

| 项 | 数值 |
|---|---|
| 轮速里程计平地漂移 | 1–2% 距离 |
| IMU gyro yaw 漂移（消费级 MEMS）| ~0.2–0.8 °/min |
| 5 秒 @ 0.3 m/s 行程 | 1.5 m |
| **→ 位置漂移** | ○ **~3–7 cm** |
| **→ yaw 漂移（轮速+陀螺）** | 🟢 ○ **<0.5°** → 横向 1.3 cm |
| **→ yaw 漂移（纯轮速，omni 轮）** | 🔴 ○ **1–3°** → 横向 2.6–7.8 cm |

> `planning_node` 栅格分辨率 **0.1 m**，所以纯轮速的横向偏差已接近一格，加陀螺才有安全余量。里程计三个变体的对比见 [`README.md`](README.md)。

| 方案 | 6 核（关 VIO）| **4.5 核（VIO 开着）** | 5 s 预算 |
|---|---|---|---|
| 0 现状 main 全 CPU | ⬤ 15.1 s | ○ ~20 s | 🔴 超 4 倍 |
| 1 ORB 全经典 | ⬤ 0.39 s | ○ ~0.5 s | ✅ **10× 余量** |
| 2 BoW+SP+**LG**（top_k=1）| ⬤ 4.5 s | ○ ~6 s | 🔴 超 |
| **5a BoW+SP+BF** | ⬤ 1.40 s | ○ **~1.8 s** | ✅ **2.8× 余量** |
| **6 DINO@BPU+SP+BF** | ○ 1.54 s | ○ **~1.9 s** | ✅ 2.6× 余量 |

**两条结论**：

1. **关 VIO 从「前置条件」降级为「优化项」** —— 方案 1/5a/6 在 4.5 核下都塞得进 5 s，**不用等 LooperHub 权限就能开工**；拿到权限只是把余量从 2.8× 提到 4×。
2. **5 s 预算独立地判死了 LightGlue** —— 唯一超预算的就是带 LG 的方案。这与「用 BF 替代 LG」的结论一致，但是一条独立的理由。

⚠️ 4.5 核那列是**外推值**（只实测了 SuperPoint 6 线程 = 1010 ms），需补测 SP 线程扩展曲线。
⚠️ **CPU 独占仍是真实约束，但已大幅缓解**：方案 5a 在 5 s 周期里只忙 1.8 s（占空比 36%），`planning_node`（需 ~1 核持续跑 10–30 Hz 的 raycasting + ESDF + 轨迹打分）有足够空间；而现状 main 的 15.1 s 会把规划饿死整个周期。
⚠️ `map_node` **必须加纯定位模式** —— 现在 `keyframe_callback`（`map_node.py:332`）每个关键帧无条件跑 `keyframe_mapping()`（写盘存 depth/image + 重算 DINO/SP + 全量 Ceres pose graph），额外 391.8 ms/帧且随地图增长。

---

## 9. GPU 对照基准

从一次真实建图统计（`output/map_jiantu2_bow/build.log`，18459 关键帧，x86 + TensorRT）：

| 阶段 | 次数 | 平均 | 最大 |
|---|---|---|---|
| **mapping_loop**（单关键帧总处理）| 18459 | **391.8 ms** | 6440 ms |
| pose_graph_trajectory_publish | 18459 | **331.7 ms** | 2861 ms |
| solve_pose_graph_online | 84 | 49.5 ms | 596 ms |
| save_image_and_depth | 18459 | 38.6 ms | 294 ms |
| **super_point_extractor（TRT）** | 18459 | **8.4 ms** | 26 ms |
| publish_local_pointcloud | 18459 | 6.3 ms | 935 ms |
| msg_decode | 18459 | 3.2 ms | 17 ms |
| build_bow_index（一次性）| 1 | **768 s** | |
| occupancy_raycast（一次性）| 1 | 136 s | |

**两个重要推论**：

**① SuperPoint：GPU 8.4 ms vs X5 CPU 1010 ms = 120 倍差距。** 这是"为 GPU 设计的算法直接搬到嵌入式 CPU"的代价。

**② 原版重定位频率不是 10 Hz，而是约 2.5 Hz。** 关键帧**生成**阈值是 3 cm / 1° / 3 秒（`looper_bridge_node.py:235`），按 0.3 m/s 理论上 10 Hz；但关键帧**处理**（`mapping_loop`）平均要 392 ms，实际上限约 **2.5 Hz**。而且 `pose_graph_trajectory_publish`（332 ms，发布 18459 个 pose 的完整 Path）和 `solve_pose_graph` 都随地图规模增长 —— **大地图上会退化到几秒一次**。

> 这条可优化：导航时没必要每个关键帧都发布完整轨迹 Path。**PR #172（junlinp，gate pose graph trajectory publishing）正是修这个的**，可直接借用。

---

## 10. 检索质量与日夜数据

### 10.1 现有基准（main 现成数据，x86，`tool/benchmark/map_retrieval_self_consistency.py`）

| 场景 | 方法 | R@1 (0.5 m) | R@10 (0.5 m) | Top1 中位误差 |
|---|---|---|---|---|
| Day | DINOv2 global (CLS) | 78.80% | 97.44% | 0.229 m |
| Day | **DINOv2 patch VLAD** | **88.87%** | **99.25%** | 0.194 m |
| Night | DINOv2 global (CLS) | 23.34% | 35.96% | **8.099 m** |
| Night | **DINOv2 patch VLAD** | **31.21%** | 39.89% | **7.382 m** |

**夜间 R@1 只有 31%，Top1 中位误差 7.4 m** —— 光照变化是检索的死穴。注意这是**跨条件**（白天地图 vs 夜晚查询），问题的最难形式。日夜策略（双时段地图）见 [`README.md`](README.md)。

### 10.2 🔴 立体相机是**可见光单色**，机身**没有主动照明**（2026-07-31 实测）

> 这条推翻了"`SC132GS` 是单色全局快门 → 大概是近红外 → 日夜差异会小得多"的猜想。**日夜问题是真实的，跑不掉。**

**⬤ 实测到的硬证据**（在正常机器桌面 B 上）：

| # | 证据 | 说明 |
|---|---|---|
| 1 | **抓到真实帧对照** | 同一时刻同场景的 stereo mono + RGB。画面里有个**绿色 LED 安全出口标志**（发射谱 ~525 nm，NIR 输出基本为零），**在 mono 立体图里清晰可见**且位置与 RGB 一致 → **镜头前没有"挡可见光只过 IR"的滤片，成像靠环境可见光** |
| 2 | 深色地毯在 mono 图里依然很暗 | 850 nm IR 下深色纺织染料通常会明显变亮，这里没有 |
| 3 | 格式确认 | topic `encoding = mono8`，**544×640**；`stereo_camera.json` 里 `format: MONO8`；MIPI datatype `0x2b` = RAW10 |
| 4 | **主动照明：全盘零命中** | `/sys/class/leds` 不存在；`strings` 搜 `libinsight_full_plugin.so`、`insight_full` 主程序、整个 `/etc/init.d/looper/` 的 `infrared|projector|illuminat|emitter|led|laser|ir_cut|ircut` → **0 命中**；全盘搜 `850nm|940nm` → 0 命中；`gpio_init.sh` 只有 GPIO 351 常置高 + GPIO 409 拉低 0.5s 再拉高（典型 reset/power-on 序列），**没有任何亮度/占空比控制** |
| 5 | **唯一的 PWM 是帧同步，不是补光** | `lpwm_config_info`：core1/core2 `period=50000 duty_time=100 occupied=CAMSYS`。**50000 → 20 Hz，正好等于立体 20 fps**，两路对应两颗 sensor；且 i2c 实测 `0x3222=0x02`、`0x3217/0x3218=0x00` 精确命中库里的 `_setting_slave`（**外触发从模式**）表 → **两路独立触发两颗 sensor 做硬同步，是立体同步信号** |

**○ 推断（未验证）**：SC132GS 这类导航相机通常**省掉 IR-cut 滤片**以提高感光度，所以它很可能是宽谱的（可见光 + NIR 都收）。但**软件层面无法证明有无 IR-cut**。

#### 对夜间导航的意义

| 场景 | 能否重定位 |
|---|---|
| 晚上**灯开着** | 🟢 和白天差异不大（室内本就人工光为主）+ 双时段地图 → 基本无损 |
| 晚上灯关、有漏光 | 🟡 极低照度，取决于 sensor 感光下限和运动模糊 |
| 晚上**全黑** | 🔴 **纯视觉直接失效，机身帮不上忙** —— 立体和深度都依赖环境可见光 |

**要做全黑环境的夜间导航，必须外加照明。** 加 850 nm 红外补光**可能**可行（前提是真的没有 IR-cut），也可以直接用可见光补光。

🔬 **最简验证（需要人在现场，1 分钟）**：**拿电视遥控器对着立体镜头按键，看 `infra1` 图里有没有亮点。**
- 有亮点 → 无 IR-cut，**红外补光方案成立**（不刺眼、不影响人）
- 没亮点 → 装了 IR-cut，只能用可见光补光

### 10.3 ⚠️ 四个数据空白（各自卡住一条路线）

| 组合 | recall 数据 | 卡住谁 |
|---|---|---|
| DINOv2 CLS / patch VLAD (fp16) | ✅ 见上表 | 基线 |
| **DINOv2 int8（量化后）** | ❌ **无数据** | 方案 6 |
| **SuperPoint BoW** | ❌ **无数据** | 方案 2 / 5 |
| **DBoW3 + ORBvoc** | ❌ **无数据** | 方案 1 / 3（**即当前基线 PR #136**）|
| **SP 描述子 + BF-L2 匹配** | ❌ **无数据** | 方案 5 / 6 共同 |

> 本文所有「预期鲁棒性 弱/中/强」的判断**全部基于算法原理推断，没有一个是实测的**。
> **这四项全部可以在 PC 上量化，不需要任何板上硬件** —— 优先级最高的工作。

---

## 11. 地图存储

一张 32×42 m 的实测地图 **38 GB**，构成：

| 组成 | 大小 | 导航时需要吗 |
|---|---|---|
| `depths.db`（每帧 depth 图）| 绝大部分 | ❌ **不需要** —— `keypoint_with_depth_to_3d`（`map_node.py:491`）只按关键点坐标采样 `depth[v,u]`，可预先算成 3D 点存下来 |
| `images.db`（每帧原图）| 大 | ❌ 导航时不需要（只建图/调试用）|
| 特征描述子 | 见下 | ✅ 需要 |
| 位姿图 + 占据栅格 | 小 | ✅ 需要 |

**特征描述子的存储差异**（每帧）：

| 方案 | 描述子 | 每帧 | 2000 关键帧 |
|---|---|---|---|
| ORB 1024×32 B 二值 | ⬤ **39 KB** | 39 KB | ~78 MB |
| SuperPoint 512×256 float32 | ⬤ **528 KB** | 528 KB | ~1.06 GB |
| DINOv2 CLS 768 float32 | 3 KB | 3 KB | ~6 MB |
| DINOv2 patch VLAD | ○ ~16 KB | ~16 KB | ~32 MB |
| SuperPoint BoW（512 词稀疏）| 2 KB | 2 KB | ~4 MB |
| DBoW3 | ○ 1.5 KB | 1.5 KB | ~3 MB |

**瘦身方案（学习式路线必须做）**：写 `tool/export_reloc_map.py` —— 丢掉 `depths.db` 和 `images.db`，只导出「关键点 3D 坐标 + 描述子 + 位姿图 + 栅格」。

| 路线 | 瘦身后 | 板上够吗 |
|---|---|---|
| ORB（方案 1）| ~**720 MB** | 🟢 标准版 2.3 G 都够 |
| SuperPoint（方案 5/6）| ~**500 MB**（丢弃部分帧）/ 未瘦身 9.5 GB | 🟢 64GB 版 44 G；🟡 标准版需瘦身 |
| **双时段（白天+夜晚）** | ~**1.5 GB** | 🟢 64GB 版毫无压力 |

---

## 12. 依赖与部署障碍

| 依赖 | 板上现状 | tinynav 要求 | 状态 |
|---|---|---|---|
| Python | 3.10.12 | 3.10.x | 🟢 |
| ROS 2 | Humble | Humble | 🟢 |
| `rclpy` | 可用 | — | 🟡 **必须先 source `/etc/init.d/looper/setting/ros2_env.conf`**（缺 `libspdlog.so.1`，在 `/userdata/hobot/opt/hobot/deps`）。⚠️ 64GB 版这招不管用，`ros2` CLI 报 `PackageNotFoundError: ros2cli` |
| `onnxruntime` | **1.18.0 板载自带** | 未声明 | 🟢 |
| `numpy` | 1.21.5 | **1.26.1** | 🔴 版本冲突 |
| `scipy` | **未安装** | 必需（EDT / cKDTree）| 🔴 |
| `numba` | **未安装** | 必需（planning JIT）| 🔴 |
| `cv2` | 系统版损坏 | 必需 | 🟡 装 `opencv-python-headless==4.6.0.66` 可用（4.8 需 numpy≥1.22）|
| `tensorrt` / `cuda` | 无 | `models_trt.py:1` 硬 import | 🔴 需 soft-import（**PR #136 已做**）|
| `pydbow3` | 无 | 方案 1 必需 | 🔴 **不在 PyPI**，aarch64 上待验 |
| `tinynav_cpp_bind` | 未编译 | 需 pybind11 + Eigen3 + **Ceres** | 🔴 需 aarch64 本地编译 |
| `lerobot` | 无 | `lekiwi_control.py` 硬 import | 🔴 **依赖 torch，装不下** → 需轻量 Feetech 驱动 |

**`pyproject.toml` 的坑**：`cuda_python==12.6.0; platform_machine == 'aarch64'` 是给 Jetson 写的 marker，X5 也是 aarch64 会命中，`uv sync` 直接装不下去。

**建议**：正式部署用独立 venv，别动系统 site-packages（`rclpy` / `cv_bridge` 的 C 扩展绑定了 numpy ABI）。

---

## 13. 上游分支盘点

| | `origin/main` | **PR #136**<br>`junlinp/add-orb-matcher` | **PR #194**<br>`xiaole/sp-bow-map-node` | `feat/add-superpoint-cpu-benchmark` |
|---|---|---|---|---|
| 全局检索 | DINOv2 **patch VLAD** | **DBoW3** + 预训练 ORBvoc (145 MB) | **SuperPoint BoW**<br>(cv2.kmeans 512 词 + TF-IDF) | — |
| 局部特征 | SuperPoint | **ORB** | SuperPoint | — |
| 匹配 | LightGlue | **BF-Hamming + RANSAC** | LightGlue | — |
| 需要的 NN | 3 | **0** | 2 | — |
| 对应方案 | 方案 0 | **方案 1** | 方案 2 的检索层 | — |
| PR 状态 | 已合并 | 🟢 **OPEN** | 🔴 **CLOSED 未合并** | 无 PR，落后 130 commit |
| 模式开关 | — | ✅ `--loop-closure-mode {embedding,bow}` | ❌ 硬替换，还删了回环检测 | — |
| TRT soft-import | ❌ 硬 `import tensorrt` | ✅ **有** | ❌ | — |
| 留下的实测数据 | ✅ day/night R@K | ❌ 无 | ❌ **一个字都没有** | ❌ 无 |

**PR #136 是对 X5 最有价值的**（本项目基线）：TRT/cuda soft-import 骨架、`DummyEmbeddingEngine`、ORB/BF 输出刻意做成 SuperPoint/LightGlue 兼容格式、DBoW3 封装、以及一套针对 `169.254.10.1` 的设备部署脚本 `scripts/setup_device_deps.sh`。

**PR #194 虽然 CLOSED，但有两个零冲突可复用的新文件**：
- `tinynav/core/bow_retrieval.py`（**148 行，只 import `cv2` + `numpy`**，cv2.kmeans 512 词 + TF-IDF）
- `tool/build_bow_index_for_map.py`（能对已建好的地图**离线补索引**）

其 `map_node.py` / `build_map_node.py` 改动与 main 的 VLAD 改动必然冲突，不要整体合。

**其他可直接借用的 PR**：

| PR | 作者 | 内容 | 价值 |
|---|---|---|---|
| **#150** | junlinp | offline map merge via cross-map loop closure | ⭐ **双时段地图合并**正需要 |
| **#172** | junlinp | gate pose graph trajectory publishing | 修掉 § 9 实测到的 332 ms 开销 |
| #167 / #171 | xiaolefang-dm | auto-localization assist（yaw sweep until localized）/ VIO guard | 重定位失败时的兜底策略 |

---

## 附录 A. 我在相机上的改动

**仅在桌面设备 A 上**（64GB 版和其他两台都没有）：

```
/userdata/tmp/            ~345 MB
├── bench/                 197 MB   dinov2/lightglue/superpoint onnx + ort_bench.py + lg_parallel.py
│                                   + match_bench.py + orb_bench.py + dino_mem.py + make_gemm.py
├── pylibs/                ~90 MB   opencv-python-headless 4.6.0.66（新装）
├── *.whl                   57 MB   两个 opencv wheel
├── stub/                            没用上的 gtk 占位符（弯路）
└── vio_trace.py/.csv                VIO 线程级采样器（已停）
```

**系统文件未做任何改动** —— `/app` 和 `/etc` 下无 2026-07-29 之后被修改的文件，未装任何系统级包，未改 `insight_full` 及其配置。

清理：`rm -rf /userdata/tmp`

**迁移建议**：⚠️ **别再手搬文件。** 现在有 ≥2 台设备、后面还要跑多个方案版本。扩展 PR #136 的 `scripts/setup_device_deps.sh`，做成能从零装好任意 Looper 的脚本。真正需要带上的只有：`superpoint_fp16_dynamic.onnx`（5 MB）、opencv-headless **4.6.0.66** whl、以及几个 KB 的 bench 脚本。DINOv2/LightGlue 的 onnx 都不用带（前者走 BPU 要的是 `.bin`，后者已放弃）。
